# 免费上线模板：GitHub Pages + Cloudflare Workers

这套模板用于快速上线“不需要自己买服务器、不依赖家里 Wi-Fi、不绑卡”的公开网站。

## 适用场景

- 展示型网站
- 数据看板
- 实时行情、天气、查询工具
- 信息聚合页
- 个人工具站

## 不适用场景

- 需要用户登录和私密数据
- 需要数据库、文件上传、支付
- 需要 WebSocket 实时长连接
- 需要长时间后台任务

## 免费额度

- GitHub Pages：免费托管网页，约 100GB/月流量
- Cloudflare Workers：免费接口，约 10 万次请求/天
- 两者注册都不需要绑银行卡

## 架构

```text
手机/电脑浏览器
      ↓
GitHub Pages（网页）
      ↓
Cloudflare Workers（接口中转）
      ↓
外部数据源（行情、天气、公开 API）
```

## 项目目录

```text
project/
  public/
    index.html
    app.js
    styles.css
  worker/
    index.js
    wrangler.toml
  .github/
    workflows/
      pages.yml
```

## 上线步骤

1. 注册 GitHub 账号并登录。
2. 新建一个仓库，把项目代码推上去。
3. 注册 Cloudflare 账号并登录（邮箱验证即可，不绑卡）。
4. 安装 Cloudflare 部署工具：

```bash
npm install -g wrangler
```

5. 登录 Cloudflare：

```bash
wrangler login
```

6. 部署接口：

```bash
cd worker
wrangler deploy
```

7. 部署完成后，把输出的地址填到 `public/index.html`：

```html
<script>
  window.__API_BASE = "https://你的接口.你的子域.workers.dev";
</script>
```

8. 推送代码，GitHub Actions 会自动部署 GitHub Pages。
9. 打开链接：

```text
https://你的GitHub用户名.github.io/你的仓库名/
```

## 新网站怎么改

- 改页面：编辑 `public/index.html`、`public/app.js`、`public/styles.css`
- 改接口：在 `worker/index.js` 里新增路由
- 前端调用接口：

```js
const API_BASE = window.__API_BASE || "";
const res = await fetch(API_BASE + "/api/你的接口");
```

## 上线检查清单

- [ ] 手机打开正常
- [ ] 电脑打开正常
- [ ] 接口能返回真实数据
- [ ] 浏览器控制台无跨域报错
- [ ] 页面没有横向滚动
- [ ] 搜索、筛选、刷新等核心功能可用
- [ ] 非自家 Wi-Fi 也能打开

## 进阶方案

- 自定义域名：可以绑定自己的域名
- 数据库：用 Supabase 免费版
- 登录系统：用 Cloudflare Access 或 Supabase Auth
- 访问量变大后：升级 Cloudflare Workers 付费版，或改用云服务器

## 本仓库中的示例

- `template/`：可直接复制使用的模板
- 当前股票看板：`https://3430750474-cloud.github.io/stock-long-dashboard/`
